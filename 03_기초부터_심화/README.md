# 기초부터 심화까지 — 따라치기 & 개념 & 예제

교수(AI)가 학생(너)에게 단계별로 설명하고, 따라 치고, 예제를 풀 수 있게 만든 코스입니다.  
**빅데이터분석기사 실기·실무**에 필요한 주제(이상치, 스케일링, 파생변수, 회귀/분류, 평가 지표, 데이터 수집)까지 포함합니다.

---

## 수강 순서 (반드시 이 순서대로)

### 기초

| 순서 | 파일 | 내용 | 난이도 |
|------|------|------|--------|
| 1 | `01_변수와출력.py` | 변수, print, f-string | ★☆☆ |
| 2 | `02_리스트와반복.py` | 리스트, for, append, len, 슬라이스 | ★☆☆ |
| 3 | `03_함수와튜플.py` | def, return, 튜플, 여러 값 반환 | ★☆☆ |

### 중급

| 순서 | 파일 | 내용 | 난이도 |
|------|------|------|--------|
| 4 | `04_파일읽기_CSV.py` | open, readlines, 헤더/행 나누기, 열 추출 | ★★☆ |
| 5 | `05_전처리_결측채우기.py` | null 판별, 평균 대치, 열 단위 처리 | ★★☆ |
| 6 | `06_pandas_집계와구간.py` | DataFrame, groupby, agg, pd.cut | ★★★ |
| 7 | `07_가설검정.py` | t검정, 카이제곱, p-value (원리+코드) | ★★★ |

### 빅분기·실무 (데이터 전처리·모형 구축·평가·수집)

| 순서 | 파일 | 내용 | 난이도 |
|------|------|------|--------|
| 8 | `08_이상치_처리.py` | IQR, Z-score, 캡핑 | ★★☆ |
| 9 | `09_표준화_정규화.py` | StandardScaler, MinMaxScaler, train 기준 fit | ★★☆ |
| 10 | `10_파생변수_생성.py` | 비율·구간·qcut·np.where | ★★☆ |
| 11 | `11_회귀_분류_모형.py` | train_test_split, 스케일링, RandomForest, LabelEncoder | ★★★ |
| 12 | `12_모형_평가_지표.py` | RMSE, MAE, R2, 정확도, F1, confusion_matrix | ★★★ |
| 13 | `13_데이터_수집.py` | JSON 파싱, DataFrame 변환, API(requests) 패턴 | ★★☆ |

---

## 필요한 패키지

- 기초~07: `pandas`, `numpy`, `scipy`
- 09, 11: `scikit-learn`
- 13에서 API 연습: `requests` (선택)

```bash
pip install pandas numpy scipy scikit-learn requests
```

---

## 각 파일 사용법

1. **개념 설명** — 파일 맨 위 주석을 읽는다. (교수 설명)
2. **따라 치기** — "따라 칠 코드" 블록을 직접 타이핑한 뒤 실행한다.
3. **예제 풀기** — "예제" 섹션에서 빈칸을 채우거나 문제를 풀고 실행해 본다.

한 파일이 끝나면 다음 파일로 넘어가면 됩니다.
