# 06. Bank 은행 마케팅 — 해설

## 풀이 흐름

1. X/y 분리 (y, ID 제거)
2. 범주형(select_dtypes object) → LabelEncoder
3. RandomForestClassifier
4. submission (ID, y)

## 핵심 포인트

- bank 데이터: job, marital, education 등 범주형 많음
- `select_dtypes(include=["object"])` 로 자동 탐지
