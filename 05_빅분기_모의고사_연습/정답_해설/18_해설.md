# 18. Weather 날씨 — 해설

## 풀이 흐름

- 시계열: `pd.to_datetime`, `dt.hour`, `dt.date`, `dt.to_period("M")`
- `groupby`로 일·월·시간대별 집계
- 두 기온 열은 같은 시각 행끼리 `corr()` 가능

## 핵심 포인트

- 문항(Q1~Q10)은 **`18_weather_날씨.py` 상단 [수행 요구사항]** 참고
- 열: `time`, 이화동/수영동 강수·기온
