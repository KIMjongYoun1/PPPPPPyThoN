# 22. Happy 행복 지수 — 해설

## 풀이 흐름

- `groupby("년도")`, `groupby("나라명")`로 집계
- 연도 비교(Q10): `pivot_table` 또는 `merge`로 2018·2019 점수 맞춘 뒤 차이
- 회귀(Q8): `LinearRegression` 또는 statsmodels `OLS` (결측 제거 후)

## 핵심 포인트

- 문항(Q1~Q10)은 **`22_happy_행복지수.py` 상단 [수행 요구사항]** 참고
- 컬럼명은 한글 — 로드 후 `df.columns` 확인
