# 21. Drug 약물 분류 — 해설

## 풀이 흐름

1. X/y 로드, ID 분리
2. X 범주형 → LabelEncoder
3. **y (Drug) 다중분류**: LabelEncoder로 숫자 변환 → 학습 → inverse_transform으로 원래 레이블 복원
4. submission (ID, Drug)

## 핵심 포인트

- **다중분류**: y가 DrugY, DrugX 등 여러 클래스
- predict 후 `le_y.inverse_transform()` 으로 문자열 레이블 복원
