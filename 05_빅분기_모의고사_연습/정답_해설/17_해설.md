# 17. Spotify 음악 — 해설

## 풀이 흐름

- `groupby`, `value_counts`, `sort_values`, `nlargest` 활용
- 열 이름에 공백이 있으면 `df["top genre"]` 형태로 접근

## 핵심 포인트

- 문항(Q1~Q10)은 **`17_spotify_음악.py` 상단 [수행 요구사항]** 참고
- `added`는 `pd.to_datetime(..., errors="coerce")`로 안전하게 파싱
